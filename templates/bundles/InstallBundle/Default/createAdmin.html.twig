{% extends '@Jinya_Install/base.html.twig' %}

{% block content %}
    {{ form_start(form) }}
    <h1>{{ 'layout.install.admin.title'|trans }}</h1>
    {{ form_widget(form) }}
    <div class="form-group row">
        <div class="offset-sm-2 col-sm-10">
            <button class="btn btn-primary" type="submit">{{ 'generic.save'|trans }}</button>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}

