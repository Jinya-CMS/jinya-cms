{% extends '@Backend/galleries/galleries_base.html.twig' %}

{% block content %}
    <form method="POST">
        <h1>{{ 'backend.galleries.delete.title'|trans({'%name%': gallery.name}) }}</h1>
        <p>{{ 'backend.galleries.delete.content'|trans({'%name%': gallery.name})|raw }}</p>
        <p>
            <button class="btn btn-danger" type="submit">{{ 'generic.delete'|trans }}</button>
            <a class="btn btn-secondary" href="{{ url('backend_galleries_overview') }}" data-target="#content"
               data-action="nav">{{ 'generic.cancel'|trans }}</a>
        </p>
    </form>
{% endblock %}
