$schema: "https://gitlab.com/gitlab-org/gitlab/-/raw/master/app/assets/javascripts/editor/schema/ci.json"

installer-prettier:
  extends:
    - .javascript-lint
  variables:
    SCRIPT: installer:prettier

installer-stylelint:
  extends:
    - .javascript-lint
  variables:
    SCRIPT: installer:stylelint

installer-eslint:
  extends:
    - .javascript-lint
  variables:
    SCRIPT: installer:eslint
