{
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "wasm-pack build --target web && yarn rollup ./main.js --format iife --file ./pkg/index.js"
  },
  "devDependencies": {
    "rollup": "^2.23.0",
    "wasm-pack": "^0.9.1"
  }
}
